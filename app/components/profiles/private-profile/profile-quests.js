// @flow

import React from 'react';

type TProfileQuests = {};

export const ProfileQuests = (props: TProfileQuests) => {
  return (
    <div className="container mt-5">
      Quests will be released serially starting in July
    </div>
  );
};
