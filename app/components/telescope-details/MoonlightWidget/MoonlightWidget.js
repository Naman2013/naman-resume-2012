import React from 'react';
import SectionHeader from '../../common/headers/SectionHeader';

const MoonlightWidget = ({ label }) => (
  <div>
    <SectionHeader title={label} />
  </div>
);

export default MoonlightWidget;
