import React from 'react';
import { shallow } from 'enzyme';

import UpcomingMissionList from './UpcomingMissionList';

describe('<UpcomingMissions />', () => {
  it('should render a list of <Missions />', () => {
    return false;
  });
});
