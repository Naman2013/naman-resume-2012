import React from 'react';
import { shallow } from 'enzyme';
import Mission from './Mission';

describe('<Mission />', () => {
  it('should render pending mission', () => {
    return;
  });

  it('should render an upcoming scheduled mission', () => {
    return;
  });
});
