import React from 'react';
import style from './telescope-offline.scss';

const TelescopeOffline = ({ imageSource }) => (
  <div>
    <h2>Telescope is offline...</h2>
  </div>
);

export default TelescopeOffline;
