import React from 'react';
import { Helmet } from 'react-helmet';
import { connect } from 'react-redux';

const PageMetaManagement = () => (
  <Helmet>
    <title>Test changing the title....</title>
  </Helmet>
);

export default PageMetaManagement;
