import React from 'react';

/**
 *
 * @param povDimension: dimension of the square viewer
 * @param fovDimension: dimension of the FOV, could be wider than POV
 * @constructor
 */

const FOV = ({ povDimension, fovDimension }) => (
  <rect />
);

export default FOV;
