import React from 'react';
import { storiesOf } from '@storybook/react';

storiesOf('Header Audio Player', module)
  .add('LIVE audio', () => (
    <div>
      <h1>Audio player story configured...</h1>
    </div>
  ));
